GIT COMMAND ( LOCAL )
- $ git init          : Menginisialisasi repo git
- $ git add <file(s)> : Menambahkan file sebuah Staging Area

  < $ git add . : Digunakan untuk memasukkan semua file yang mengalami perubahan kedalam staging area >
  
- $ git status        : Mengetahui status repo ( ada file baru,hilang,dll )
- $ git commit        : Membuat sebuah commit

  < $ git commit -m "message / keterangan yang ingin ditambahkan" >
  < $ git commit -am "message" : digunakan untuk meng commit file modified / telah di add sebelumnya>
  
- $ git config        : Memasukkan configuration ke dalam git

  < $ git config  --global user.name "username yang ingin dimasukkan" >     } Usahakan sesuai dengan github untuk lebih memudahkan
  < $ git config  --global user.email "email yang anda gunakan" >           } Usahakan sesuai dengan github untuk lebih memudahkan
  
- $ git branch        : Membuat sebuah branch

  < $ git branch -d <nama_branch> : menghapus branch >
  < $ git branch --merged : mengetahui branch mana saja yang sudah di gabung > 
  
  
- $ git help          : Membantu kita untuk mengetahui penggunaan GIT
- $ pwd               : Mengetahui lokasi di folder saat ini
- $ ls                : Mengetahui semua folder yang ada
- $ cd <folder(X)     : Berpindah folder
- $ git log           : Memunculkan semua commit yang telah di lakukan

  < $ git log -3 : memunculkan 3 commit terakhir ( "3" bisa di ganti ) >
  < $ git log --<file(s)> : memunculkan commit sebuah file secara spesifik >
  
- $ alias <nama_alias> = "command" : Membuat nama lain / alias untuk sebuah command yang panjang
- $ git merge <nama_branch> : menggabungkan branch

3 AREA PADA REPO GIT
(1) Working Area
(2) Staging Area ---> Area untuk menyimpan semua perubahan yang ada di working area dengan ( $ git add )                         } disimpan di file dengan nama .git
(3) History      ---> Area untuk menyimpan commit dari perubahan yang ada di staging area dengan ( $ git commit )                } disimpan di file dengan nama .git

JENIS MERGE
1. Fast Forward       : menggabungkan branch yang berada dijalur langsung / direct path
2. three way merge    : menggabungkan branch yang tidak berada pada
                        direct path / jalur yang menghubungkan langsung
